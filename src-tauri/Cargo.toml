[workspace]
members = [
    "crates/practice-domain",
    "crates/audio-engine",
    "crates/speech-engine",
    "crates/assessment-engine",
    "crates/content-engine",
    "crates/storage",
]

[package]
name = "tts-vue-next"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "tts_vue_next_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# workspace crates
practice-domain = { path = "crates/practice-domain" }
audio-engine = { path = "crates/audio-engine" }
speech-engine = { path = "crates/speech-engine" }
assessment-engine = { path = "crates/assessment-engine" }
content-engine = { path = "crates/content-engine" }
storage = { path = "crates/storage" }

# existing dependencies
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = { version = "0.26", features = ["native-tls"] }
reqwest = { version = "0.12", features = ["json"] }
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }
rand = "0.9"
chrono = "0.4"
futures-util = "0.3"
thiserror = "2"
url = "2"
log = "0.4"
env_logger = "0.11"
base64 = "0.22"
regex = "1"
pulldown-cmark = "0.12"
docx-rs = "0.4"
async-trait = "0.1"

# Phase 1: practice
hound = "3.5"

