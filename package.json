{
  "name": "tts-vue-next",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "ffmpeg:sync": "node sync-ffmpeg-sidecar.mjs",
    "postinstall": "pnpm ffmpeg:sync",
    "tauri": "pnpm ffmpeg:sync && tauri",
    "tauri:dev": "pnpm ffmpeg:sync && tauri dev",
    "tauri:build": "pnpm ffmpeg:sync && tauri build",
    "tauri:build:win": "pnpm ffmpeg:sync && tauri build --target x86_64-pc-windows-msvc",
    "tauri:build:mac": "pnpm ffmpeg:sync && tauri build --target universal-apple-darwin",
    "tauri:build:mac:intel": "pnpm ffmpeg:sync && tauri build --target x86_64-apple-darwin",
    "tauri:build:mac:arm": "pnpm ffmpeg:sync && tauri build --target aarch64-apple-darwin",
    "tauri:build:linux": "pnpm ffmpeg:sync && tauri build --target x86_64-unknown-linux-gnu"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.0",
    "@tauri-apps/plugin-fs": "^2.5.0",
    "@tauri-apps/plugin-opener": "^2",
    "pinia": "^3.0.4",
    "pinia-plugin-persistedstate": "^4.7.1",
    "vue": "^3.5.13",
    "vue-i18n": "^11.3.2",
    "vue-router": "^4.6.4",
    "vuetify": "^3.12.5",
    "vuetify-message-vue3": "^1.1.2"
  },
  "devDependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/test-utils": "^2.4.6",
    "happy-dom": "^20.9.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.4",
    "vue-tsc": "^2.1.10"
  }
}
